import React from 'react';
import Root from '@/pages/root';
import Header from '@/components/header';

const App: React.FC = () => {
  return (
    <div>
      <Header />
      <Root />
    </div>
  );
};

export default App;
